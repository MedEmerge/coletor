# Identidade do agente — os DOIS únicos campos que ficam no hospital.
# Schedule, credenciais do banco e queries vêm do servidor a cada ciclo
# (docs/arquitetura.md §3.3), então mudar a coleta não exige tocar aqui.
#
# Copie para credentials.yaml e preencha com o que a plataforma mostrou em
# /configuracoes/coletor. O agent_secret aparece uma única vez.

public_id: '00000000-0000-0000-0000-000000000000'
agent_secret: 'ag_cole-o-segredo-aqui'
